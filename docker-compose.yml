
networks:
  platform:
    driver: bridge

volumes:
  sqlserver_data:
  chroma_data:

services:

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    restart: unless-stopped
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: ${MSSQL_SA_PASSWORD:-Unprompted@db}
      MSSQL_PID: Developer                  
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks: [platform]
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"${MSSQL_SA_PASSWORD:-Unprompted@db}\" -Q 'SELECT 1' -No -C || exit 1"
        ]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 30s

  

  backend:
    build:
      context: ./Backend
      dockerfile: DOCKERFILE
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=Unprompted;User Id=sa;Password=${MSSQL_SA_PASSWORD:-Unprompted@db};TrustServerCertificate=True;
      - ConnectionStrings__Redis=redis:6379
      - JwtSettings__Secret=${JWT_SECRET:-super-secret-dev-key-change-in-prod}
      - JwtSettings__ExpiryMinutes=60
      - AiService__BaseUrl=http://ai:8000
    depends_on:
      sqlserver:
        condition: service_healthy
      
    networks: [platform]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5

  

  frontend:
    build:
      context: ./Frontend
      dockerfile: DOCKERFILE
      args:
        VITE_API_URL: http://localhost:5000
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks: [platform]

   # 1. AJOUT DE CHROMADB (Base vectorielle)
  chroma-db:
    image: chromadb/chroma:latest
    container_name: chroma_vector_db
    restart: unless-stopped
    ports:
      - "8001:8000" 
    volumes:
      - chroma_data:/chromadb/data
    environment:
      - IS_PERSISTENT=TRUE
    networks: [platform]

  # 2. AJOUT DE MICROSERVICE RAG (Python)
  ai-service:
    build:
      context: ./AI-Service 
      dockerfile: Dockerfile
    container_name: unprompted_rag
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - CHROMA_SERVER_HOST=chroma-db
      - CHROMA_SERVER_HTTP_PORT=8000
      # Tu pourras ajouter ici ton API KEY OpenAI ou autre
    depends_on:
      - chroma-db
    networks: [platform]
